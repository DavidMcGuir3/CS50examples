// example of a nested loop

#include <cs50.h>
#include <stdio.h>
#include <math.h>

int main(void)
{
    int height = 3;
    //this one controls my rows
    for (int i = 1; i <= x; i++)
    {

        //this one controls my columns
        for (int j = 1; j <= x; j++)
        {
            printf("six ");
            //printf("\n");
        }
        printf("\n");
    }
}