//What is a nested loop?

#include <cs50.h>
#include <stdio.h>
#include <math.h>

int main(void)
{
    int height = get_input("What is the height: ");

    for (int i = 1; i <= height; i++)
    {

        for (int j = 1; j <= height; j++)
        {
            if (____________)
            {
                printf(" ");
            {
            else
            {
                printf("#")
            }

        }
    printf("\n");
    }
}